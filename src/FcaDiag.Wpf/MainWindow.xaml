<Window x:Class="FcaDiag.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="FCA Diagnostics Tool - [Disconnected]"
        Height="750" Width="1200"
        MinHeight="650" MinWidth="1000"
        WindowStartupLocation="CenterScreen"
        Background="#FF0D1117">

    <Window.Resources>
        <!-- Orange accent color -->
        <Color x:Key="AccentColor">#FFFF6B00</Color>
        <Color x:Key="AccentLightColor">#FFFF8C33</Color>
        <Color x:Key="AccentDarkColor">#FFCC5500</Color>
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
        <SolidColorBrush x:Key="AccentLightBrush" Color="{StaticResource AccentLightColor}"/>

        <!-- Orange Button Style -->
        <Style x:Key="OrangeButton" TargetType="Button">
            <Setter Property="Background" Value="#FFFF6B00"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <ContentPresenter VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FFFF8C33"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FFCC5500"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#FF3A3A3A"/>
                                <Setter Property="Foreground" Value="#FF666666"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Teal/Cyan Button Style -->
        <Style x:Key="TealButton" TargetType="Button">
            <Setter Property="Background" Value="#FF00A5A5"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF00BFBF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF008080"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#FF3A3A3A"/>
                                <Setter Property="Foreground" Value="#FF666666"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Dark Button Style -->
        <Style x:Key="DarkButton" TargetType="Button">
            <Setter Property="Background" Value="#FF21262D"/>
            <Setter Property="Foreground" Value="#FF8B949E"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#FF30363D"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF30363D"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF161B22"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Panel Style -->
        <Style x:Key="DarkPanel" TargetType="Border">
            <Setter Property="Background" Value="#FF161B22"/>
            <Setter Property="BorderBrush" Value="#FF30363D"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="180"/>
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <Border Background="#FF161B22" Padding="15,10">
            <Grid>
                <StackPanel Orientation="Horizontal">
                    <Ellipse Width="12" Height="12" Fill="#FF666666" Margin="0,0,5,0"/>
                    <Ellipse Width="12" Height="12" Fill="#FF666666" Margin="0,0,5,0"/>
                    <Ellipse Width="12" Height="12" Fill="#FF666666"/>
                </StackPanel>
                <TextBlock x:Name="txtTitle" Text="FCA Diagnostics v1.0 - [Disconnected]"
                           Foreground="White" FontSize="13" HorizontalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- Main Terminal/Log Area -->
        <Border Grid.Row="1" Style="{StaticResource DarkPanel}" Margin="10,5,10,5">
            <ScrollViewer x:Name="scrollLog" VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="txtTerminal"
                           FontFamily="Consolas"
                           FontSize="13"
                           Foreground="#FF00FFAA"
                           TextWrapping="Wrap"
                           Text="> System Ready...&#x0a;> Waiting for vehicle connection..."/>
            </ScrollViewer>
        </Border>

        <!-- Action Buttons Row -->
        <Border Grid.Row="2" Background="#FF161B22" Padding="10,5">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="btnConnect" Content="Connect Pod" Style="{StaticResource DarkButton}"
                        Width="140" Click="BtnConnect_Click"/>
                <Button x:Name="btnReadEprom" Content="Read EPROM" Style="{StaticResource OrangeButton}"
                        Width="160" Click="BtnReadEprom_Click"/>
                <Button x:Name="btnSwapHw" Content="Swap HW" Style="{StaticResource TealButton}"
                        Width="140" Click="BtnSwapHw_Click"/>
                <Button x:Name="btnWriteEprom" Content="Write EPROM" Style="{StaticResource OrangeButton}"
                        Width="160" Click="BtnWriteEprom_Click"/>
                <Button x:Name="btnScanModules" Content="Scan Modules" Style="{StaticResource TealButton}"
                        Width="140" Click="BtnScanModules_Click"/>
                <Button x:Name="btnReadDtc" Content="Read DTCs" Style="{StaticResource TealButton}"
                        Width="120" Click="BtnReadDtc_Click"/>
            </StackPanel>
        </Border>

        <!-- Bottom Panels -->
        <Grid Grid.Row="3" Margin="5,0,5,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Battery Voltage Monitor -->
            <Border Grid.Column="0" Style="{StaticResource DarkPanel}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="BATTERY VOLTAGE MONITOR (V)"
                               Foreground="#FF8B949E" FontSize="11" FontWeight="SemiBold"
                               Margin="0,0,0,10"/>

                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="40"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Y-Axis Labels -->
                        <StackPanel VerticalAlignment="Stretch">
                            <TextBlock Text="15.0" Foreground="#FF666666" FontSize="10"/>
                            <TextBlock Text="14.5" Foreground="#FF666666" FontSize="10" Margin="0,25,0,0"/>
                            <TextBlock Text="14.0" Foreground="#FF666666" FontSize="10" Margin="0,25,0,0"/>
                        </StackPanel>

                        <!-- Voltage Graph Area -->
                        <Border Grid.Column="1" BorderBrush="#FF30363D" BorderThickness="1" Background="#FF0D1117">
                            <Canvas x:Name="canvasVoltage" ClipToBounds="True">
                                <!-- Grid lines drawn in code-behind -->
                            </Canvas>
                        </Border>
                    </Grid>
                </Grid>
            </Border>

            <!-- EPROM Memory Buffer -->
            <Border Grid.Column="1" Style="{StaticResource DarkPanel}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="EPROM MEMORY BUFFER"
                               Foreground="#FF8B949E" FontSize="11" FontWeight="SemiBold"/>

                    <!-- Legend -->
                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,10">
                        <Rectangle Width="20" Height="12" Fill="#FF00A5A5" Margin="0,0,5,0"/>
                        <TextBlock Text="Source Buffer" Foreground="#FF8B949E" FontSize="10" Margin="0,0,15,0"/>
                        <Rectangle Width="20" Height="12" Fill="#FF00FF88" Margin="0,0,5,0"/>
                        <TextBlock Text="Target Written" Foreground="#FF8B949E" FontSize="10"/>
                    </StackPanel>

                    <!-- Memory Buffer Bars -->
                    <Canvas x:Name="canvasMemory" Grid.Row="2" ClipToBounds="True">
                        <!-- Bars drawn in code-behind -->
                    </Canvas>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
